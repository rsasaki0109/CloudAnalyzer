# Generated by `scripts/prepare_newer_college_mini.py` from a local
# Newer College Dataset download. The raw data is too large and the
# dataset license does not permit redistribution, so we keep the
# generated suite out of the public tree. Users prepare it locally.
data/
suite.yaml
scans/
