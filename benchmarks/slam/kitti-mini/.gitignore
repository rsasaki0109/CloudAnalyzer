# Generated by `scripts/prepare_kitti_mini.py` from a local KITTI
# Odometry download. The raw data is too large and the dataset license
# (CC BY-NC-SA 3.0) does not permit redistribution, so we keep the
# generated suite out of the public tree. Users prepare it locally.
data/
suite.yaml
scans/
